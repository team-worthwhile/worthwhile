function Integer division(Integer a, Integer b) {
	Integer c := b
	Integer d := 0
	
	if( b ≥ 0) {
		while(c ≤ a) {
			c := c + b
			d := d + 1
		}
	}

	// Note that division(x, 0) is 0 ???
	return d
}
